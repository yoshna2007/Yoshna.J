#include<stdio.h>

int main()
{
    int arr[100], n, i, j, count;

    scanf("%d",&n);

    for(i=0;i<n;i++)
        scanf("%d",&arr[i]);

    for(i=0;i<n;i++)
    {
        count = 1;

        for(j=i+1;j<n;j++)
        {
            if(arr[i]==arr[j])
                count++;
        }

        printf("%d occurs %d times\n", arr[i], count);
    }
}
