#include <stdio.h>
int main() {
    int a[5]={-2,1,-3,4,5}, max=a[0], curr=a[0];
    for(int i=1;i<5;i++){
        if(curr+a[i]>a[i])
            curr=curr+a[i];
        else
            curr=a[i];
        if(curr>max)
            max=curr;
    }
    printf("Max Sum = %d", max);
}
