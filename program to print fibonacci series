#include <stdio.h>
int main()
{
    int i = 0, first = 1, next, second = 1;

    printf("%d %d ", a, b);

    while(i <= 7)
    {
        next= first + second;
        printf("%d ", c);
        first = second;
        second = next;
        i++;
    }

    return 0;
}
