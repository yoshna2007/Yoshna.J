#include <stdio.h>
int main()
{
    int a, y, m, d;
    printf("Enter number of days: ");
    scanf("%d", &a);
    y = a / 365;
    m = (a % 365) / 30;
    d = (a % 365) % 30;
    printf("Years y = %d\n", y);
    printf("Months m = %d\n", m);
    printf("Days d = %d", d);
return 0;
}
