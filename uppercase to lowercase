#include <stdio.h>

int main()
{
    char s[50];
    int i = 0;

    printf("Enter a string: ");
    scanf("%s", s);

    while(s[i] != '\0')
    {
        if(s[i] >= 'A' && s[i] <= 'Z')
            s[i] = s[i] + 32;
        i++;
    }

    printf("Lowercase string: %s", s);

    return 0;
}
